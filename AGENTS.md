# 轻笺 AI 开发入口

默认用中文沟通。本文件保留项目授权边界、规则入口与容易遗漏的硬约束；详细工程规范由 [docs/README.md](./docs/README.md) 指定的唯一事实源维护。

## 上下文加载

- 每个新任务先确认本文件与 `git status --short`，区分本次范围和用户已有改动。当前上下文已有完整、未变化的内容不重复读取。
- 先读 [文档目录](./docs/README.md) 确认职责，再按下表加载相关章节。用标题、模块名、文件名或错误码搜索，读取命中章节的完整上下文；任务跨模块或证据不足时扩大阅读范围。禁止把搜索未命中当作不存在约束。

| 任务 | 必须加载 |
| --- | --- |
| 代码开发、诊断、评审 | [开发规范](./docs/development.md) 的实施前门禁、相关前/后端规则与自检验证；[架构](./docs/architecture.md) 的系统概览、相关领域和链路；按模块搜索 [踩坑约束](./docs/pitfalls.md) |
| 用户可见 UI | 上述工程规范，加 [设计系统](./docs/design.md) 的设计原则、主题、组件、状态、响应式和受影响页面模式；移动改动加开发规范的共享渲染基线 |
| 纯文档、Codex 规则或 Skill | 文档目录、目标文件及其引用/调用方；涉及开发或发布门禁时再读对应规范，无需默认加载全部产品架构和 UI 设计 |
| 模块化 AI | [AI Skills](./docs/ai-skills.md)，不以旧 Agent V2/V3 方案作为实现依据 |
| 预览、发布、Worker、Schema、线上操作 | [本地预览与上线](./docs/release-acceptance.md) 的相关门禁；实际发布前完整读取该流程 |
| Android / 鸿蒙真机联调 | 开发规范的真机 HMR 章节与 [Android 说明](./apps/android/README.md) |

`ln-ai-intro.md` 是本机私密上下文，仅在任务需要本机环境、账号、服务器或数据库信息时读取相关章节；普通代码、UI 和文档任务不默认读取。需要时缺失则说明，继续不依赖它的工作。禁止将密码、密钥、Token、连接串或私密账号信息写入回答、日志、补丁、提交，禁止提交该文件或取消 Git 忽略。读取不构成线上操作授权。

## 授权与交付边界

- 用户只要求分析、检查、评审、诊断或出方案时保持只读。明确要求修改或“可以优化就优化”时，在所述范围内实施与验证，不重复请求同一授权，不顺带扩大业务范围。
- 远程只读检查按全局协作偏好的长期授权执行，不重复询问。提交、推送、部署或线上写入须有当前任务对应授权；完整上线授权与分支流程见 [发布规范](./docs/release-acceptance.md#上线授权)。普通开发请求不自动授权这些动作。
- “上线/部署”包含当前工作树中能确认属于本项目的安全改动，按发布规范完成检查、提交、推送、普通分支合并、从 `main` 部署与健康检查；无需再等预览确认。来源不明或明显无关改动先停止说明；不包含私密文件、证书、构建产物、临时文件及未说明的线上迁移或批量修复。
- 不覆盖或回退用户已有改动；冲突、分支保护拒绝或主分支工作树不安全时停止，禁止强推或破坏性覆盖。

## UI 硬约束

- 有自研 B 组件的场景一律复用，禁止原生对应控件；不新增 Ant Design Vue `a-*`，存量在改动范围内替换。清单：`apps/web/src/components/base/BasicComponents/`；确认框使用 `Alert.alert()`。确无对应 B 组件时才用原生控件，并在交付中说明。
- 修改任何使用图标的代码前，实际打开 `apps/web/src/config/icon.ts`，确认完整属性路径与资源存在。新增静态 UI 图标先检查复用，缺失时在该文件定义，由 `SvgIcon` 引用；检查取值非空且不落入 `icon.nullImg`。禁止在模板新增静态 `<svg>/<path>` 或仅包装静态 SVG 的组件。单色优先 `currentColor`；动态图标、favicon、多色品牌、用户内容、复杂插画和数据可视化按各自职责处理。
- 移动浏览器、PWA 与 Android App 共用 `html.light-note-mobile-rendering`；`light-note-android-webview` 只标识引擎，禁止承载用户可见样式或业务 UA 分支。关键状态同时使用实色描边、圆点/图标或明确文字色，不只依赖混色和阴影。
- 不使用 `@supports (color-mix(...))` 分流；关键布局为 `:has()`、容器查询和动态视口单位提供等价回退，细则见 [共享渲染基线](./docs/development.md#移动浏览器--android-app-共享渲染基线)。

## 实施与验收

- 非机械改动前明确目标、根因和约束，比较局部实现与复用既有能力/唯一事实源两条路径；简单改动简短说明取舍即可，无需另写方案文档。
- 用户可见 UI 在编码前建立适用状态矩阵，覆盖默认、hover/focus、选中、完成、待领取/已领取、空、加载、错误中的相关项，并对照改动前与相邻组件。语义色优先用于图标、胶囊和状态文字，不用整卡变色代替层级。
- 逐文件通读本轮 diff 与理解行为所需的上下文；新文件完整阅读，既有文件按变更涉及的入口、调用关系和状态链补读。检查边界、空值、权限、状态重置、接入及删除残留，发现问题先修复。完整自检项见 [开发规范](./docs/development.md#自检与验证)。
- 前端至少受影响测试加类型检查或构建；后端至少语法检查或受影响测试；共享协议、AI、Schema、Worker 按 [变更级验证](./docs/release-acceptance.md#变更级验证) 执行门禁。纯规则与文档修改检查链接、冲突、敏感信息和 `git diff --check`，无需业务构建。
- 用户可见 UI 必须真实浏览器验收受影响 PC/移动端、浅色/深色和关键状态；移动基线用 `?renderProfile=mobile`，同 CSS 视口对照移动浏览器与 Debug App。结构测试不能替代视觉验收，环境不可用时明确未验收项，不声称全部完成。
- 交付简述文件、关键取舍、自检与实际验证、剩余风险；UI 补充端型、主题与状态覆盖。

## 文档治理

按 [文档目录的写作规则](./docs/README.md#写作规则) 更新唯一事实源，只保留长期有效约束。普通修复、测试结果和过程记录留在任务中；`pitfalls.md` 仅收录满足其专用门槛的已确认根因。`docs/plan/`、`docs/done/`、`docs/fix/` 是本地临时材料，只在用户引用时读取。正式文档修改后通读受影响章节，检查重复、过时描述、链接/锚点和敏感信息。
