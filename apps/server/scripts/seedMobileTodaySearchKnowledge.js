/**
 * 同步「移动端今日 + 全局搜索」改版的 AI 知识库条目。
 *
 * 按标题幂等：已存在则更新正文，不存在才新建，可重复执行。
 * 用法：node scripts/seedMobileTodaySearchKnowledge.js
 */
import pool from '../db/index.js';
import { upsertKnowledgeBase } from '../util/services/knowledgeBaseService.js';

const ENTRIES = [
  {
    title: '移动端全局搜索怎么用',
    category: '帮助中心',
    status: 'public',
    type: 'markdown',
    content: `# 移动端全局搜索

移动端只有一个文本搜索入口：**顶部搜索框**。它始终搜索整个轻笺，不会因为你在哪个页面而变成局部搜索。

## 能搜到什么

书签、笔记、文件、标签，以及**待办**。待办可以被搜索到并直接打开，但它是行动对象而不是资料，所以不会出现在资料四页签、@ 资源选择器、标签操作和待整理里。

## 入口形态

- 今日、资料、待办页：顶部是完整的宽搜索框
- AI、我的页：顶部是一个放大镜图标

两种形态打开的是同一个搜索层，能力完全一样。

## 搜索结果

输入关键词后直接看到**混合排列**的最佳匹配，不需要先选类型。结果最多 8 条、同一类型最多 3 条，避免一种类型占满。每条结果带类型标识和摘要。

点「查看全部搜索结果」进入完整搜索页，那里可以用筛选抽屉按资源类型、时间范围、标签、待办状态和待办优先级进一步收窄。待办状态和优先级只在筛选中选中了待办时才出现。

## 排序说明

当前页面只影响排序，不改变搜索范围。例如在待办页搜索时，待办结果会稍微靠前，但书签、笔记、文件和标签仍然都会展示。

## 其他

- 最近搜索保存在本机，最多 8 条，不上传服务器，不同账号互不影响
- Android 上按返回键会先关闭搜索层，而不是退出当前页面
- 各资料页不再有第二个搜索框，页面内保留的是文件夹、标签、状态、排序等结构化筛选`,
  },
  {
    title: '移动端「今日」页面是什么',
    category: '帮助中心',
    status: 'public',
    type: 'markdown',
    content: `# 移动端「今日」

「今日」是移动端底部导航的第一个入口，也是每天打开轻笺后处理事情的第一站。

## 它展示什么

- 日期与问候，以及今天应优先处理多少件事
- 待处理总览：未完成待办 / 待整理 / 未读通知，统计口径与桌面工作台一致，点击可进入对应列表或通知中心
- 快速记录：记一句、待办、保存链接、上传
- 今日待办与待整理明细，可以直接完成、延期到明天、编辑或标记整理完成；逾期待办会在这里明确标出
- 继续处理：最近编辑的笔记和最近上传的文件，最多 2 条

Android App 在列表顶部支持下拉刷新，松开后会同时刷新今日摘要、行动项和成长任务。普通手机浏览器继续使用页面内刷新入口，不接管浏览器自身的页面回弹。

## 它不展示什么

资源总量、增长趋势、文件类型分布、常用标签排行和最近更新不在移动端今日页，它们仍然保留在**桌面端工作台**。移动端今日只回答「我今天先做什么、有哪些资料还没整理」。

## 与桌面工作台的关系

两者是同一个地址 /workbenches：桌面端打开是完整工作台，移动端打开是今日。`,
  },
  {
    title: '移动端底部导航有哪些入口',
    category: '帮助中心',
    status: 'public',
    type: 'markdown',
    content: `# 移动端底部导航

从左到右依次是：**今日、资料、AI、待办、我的**。AI 位于中间并保持视觉强调。

搜索不再占底部位置，它已升级为顶部的全局搜索，覆盖全部模块。

## 各入口说明

- **今日**：今天要处理的待办、待整理和继续处理
- **资料**：书签、笔记库、云空间、标签四个页签；再次点击会回到最近浏览的那个页签，切换页签后内容从顶部开始
- **AI**：轻笺智域问答
- **待办**：待办列表、议程和日历
- **我的**：个人资料、成长、设置、资源中心、回收站等

## 相关行为

- 左上角 Logo 固定回到「今日」
- 「待整理」属于资料处理，从「我的 → 资源中心 → 待整理」进入，底部会高亮「资料」而不是「待办」
- 「默认首页」设置只在桌面端出现，移动端不需要设置
- 底部一级入口之间切换不会形成返回历史，新页面从顶部开始（AI 会话仍定位最新消息）。Android App 在一级页使用系统返回时只会回到桌面并保留当前页面进程，再次打开直接恢复；详情页照常返回上一级，弹框或抽屉打开时则优先关闭浮层`,
  },
];

async function main() {
  const [[root]] = await pool.query(
    "SELECT id FROM user WHERE role = 'root' AND del_flag = 0 ORDER BY create_time LIMIT 1",
  );
  if (!root?.id) throw new Error('未找到 root 用户，无法写入知识库');

  for (const entry of ENTRIES) {
    const result = await upsertKnowledgeBase({ userId: root.id, input: entry });
    console.log(`[knowledge] ${result.action} → ${result.title}`);
  }
  console.log(`[knowledge] 完成，共 ${ENTRIES.length} 条`);
}

main()
  .then(() => pool.end())
  .catch(async (error) => {
    console.error('[knowledge] 写入失败:', error?.message || error);
    await pool.end().catch(() => {});
    process.exitCode = 1;
  });
